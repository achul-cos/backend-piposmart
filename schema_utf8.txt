*************************** 1. row ***************************
       Table: subscriptions
Create Table: CREATE TABLE `subscriptions` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `code` varchar(80) COLLATE utf8mb4_unicode_ci NOT NULL,
  `owner_id` bigint unsigned DEFAULT NULL,
  `outlet_id` bigint unsigned DEFAULT NULL,
  `order_id` bigint unsigned DEFAULT NULL,
  `package_id` bigint unsigned DEFAULT NULL,
  `plan_id` bigint unsigned DEFAULT NULL,
  `status` varchar(40) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'ACTIVE',
  `active_from` date NOT NULL,
  `active_until` date NOT NULL,
  `total_duration_days` int NOT NULL,
  `source_type` varchar(40) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'SUBSCRIPTION_ORDER',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uq_subscriptions_code` (`code`),
  UNIQUE KEY `uq_subscriptions_order` (`order_id`),
  KEY `idx_subscriptions_owner_status` (`owner_id`,`status`),
  KEY `idx_subscriptions_active_until` (`active_until`),
  KEY `idx_subscriptions_deleted_at` (`deleted_at`),
  KEY `fk_subscriptions_outlet` (`outlet_id`),
  KEY `fk_subscriptions_package` (`package_id`),
  KEY `fk_subscriptions_plan` (`plan_id`),
  CONSTRAINT `fk_subscriptions_order` FOREIGN KEY (`order_id`) REFERENCES `subscription_orders` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscriptions_outlet` FOREIGN KEY (`outlet_id`) REFERENCES `outlets` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscriptions_owner` FOREIGN KEY (`owner_id`) REFERENCES `owners` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscriptions_package` FOREIGN KEY (`package_id`) REFERENCES `subscription_packages` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscriptions_plan` FOREIGN KEY (`plan_id`) REFERENCES `subscription_plans` (`id`) ON DELETE SET NULL,
  CONSTRAINT `chk_subscriptions_duration_positive` CHECK ((`total_duration_days` > 0)),
  CONSTRAINT `chk_subscriptions_status` CHECK ((`status` in (_utf8mb4'ACTIVE',_utf8mb4'EXPIRED',_utf8mb4'CANCELED')))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
*************************** 1. row ***************************
       Table: subscription_orders
Create Table: CREATE TABLE `subscription_orders` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `code` varchar(80) COLLATE utf8mb4_unicode_ci NOT NULL,
  `owner_id` bigint unsigned DEFAULT NULL,
  `outlet_id` bigint unsigned DEFAULT NULL,
  `closing_id` bigint unsigned DEFAULT NULL,
  `sales_id` bigint unsigned DEFAULT NULL,
  `supervisor_id` bigint unsigned DEFAULT NULL,
  `wallet_account_id` bigint unsigned DEFAULT NULL,
  `wallet_transaction_id` bigint unsigned DEFAULT NULL,
  `package_id` bigint unsigned DEFAULT NULL,
  `plan_id` bigint unsigned DEFAULT NULL,
  `promotion_id` bigint unsigned DEFAULT NULL,
  `package_snapshot_json` json NOT NULL,
  `plan_snapshot_json` json NOT NULL,
  `promotion_snapshot_json` json DEFAULT NULL,
  `tenure_months` int NOT NULL,
  `duration_days` int NOT NULL,
  `base_price` decimal(18,2) NOT NULL,
  `discount_amount` decimal(18,2) NOT NULL DEFAULT '0.00',
  `additional_charge` decimal(18,2) NOT NULL DEFAULT '0.00',
  `final_amount` decimal(18,2) NOT NULL,
  `balance_shortfall_amount` decimal(18,2) DEFAULT NULL,
  `currency` varchar(3) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'IDR',
  `order_type` varchar(40) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'NEW',
  `status` varchar(40) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT 'PAID',
  `source_subscription_id` bigint unsigned DEFAULT NULL,
  `upgrade_effective_start_date` date DEFAULT NULL,
  `upgrade_original_end_date` date DEFAULT NULL,
  `upgrade_remaining_days` int DEFAULT NULL,
  `upgrade_daily_price` decimal(18,2) DEFAULT NULL,
  `previous_package_snapshot_json` json DEFAULT NULL,
  `previous_plan_snapshot_json` json DEFAULT NULL,
  `idempotency_key` varchar(160) COLLATE utf8mb4_unicode_ci NOT NULL,
  `external_reference` varchar(120) COLLATE utf8mb4_unicode_ci DEFAULT NULL,
  `purchased_at` datetime(6) NOT NULL,
  `subscription_start_date` date NOT NULL,
  `note` text COLLATE utf8mb4_unicode_ci,
  `created_by_user_id` bigint unsigned DEFAULT NULL,
  `updated_by_user_id` bigint unsigned DEFAULT NULL,
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uq_subscription_orders_code` (`code`),
  UNIQUE KEY `uq_subscription_orders_idempotency` (`idempotency_key`),
  UNIQUE KEY `uq_subscription_orders_external_reference` (`external_reference`),
  KEY `idx_subscription_orders_owner_purchased` (`owner_id`,`purchased_at`),
  KEY `idx_subscription_orders_closing` (`closing_id`),
  KEY `idx_subscription_orders_sales_purchased` (`sales_id`,`purchased_at`),
  KEY `idx_subscription_orders_status_purchased` (`status`,`purchased_at`),
  KEY `idx_subscription_orders_wallet_transaction` (`wallet_transaction_id`),
  KEY `idx_subscription_orders_deleted_at` (`deleted_at`),
  KEY `fk_subscription_orders_outlet` (`outlet_id`),
  KEY `fk_subscription_orders_supervisor` (`supervisor_id`),
  KEY `fk_subscription_orders_wallet_account` (`wallet_account_id`),
  KEY `fk_subscription_orders_package` (`package_id`),
  KEY `fk_subscription_orders_plan` (`plan_id`),
  KEY `fk_subscription_orders_promotion` (`promotion_id`),
  KEY `fk_subscription_orders_created_by` (`created_by_user_id`),
  KEY `fk_subscription_orders_updated_by` (`updated_by_user_id`),
  KEY `idx_subscription_orders_order_type` (`order_type`),
  KEY `idx_subscription_orders_source_subscription` (`source_subscription_id`),
  CONSTRAINT `fk_subscription_orders_closing` FOREIGN KEY (`closing_id`) REFERENCES `sales_closings` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_created_by` FOREIGN KEY (`created_by_user_id`) REFERENCES `users` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_outlet` FOREIGN KEY (`outlet_id`) REFERENCES `outlets` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_owner` FOREIGN KEY (`owner_id`) REFERENCES `owners` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_package` FOREIGN KEY (`package_id`) REFERENCES `subscription_packages` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_plan` FOREIGN KEY (`plan_id`) REFERENCES `subscription_plans` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_promotion` FOREIGN KEY (`promotion_id`) REFERENCES `promotions` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_sales` FOREIGN KEY (`sales_id`) REFERENCES `users` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_source_subscription` FOREIGN KEY (`source_subscription_id`) REFERENCES `subscriptions` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_supervisor` FOREIGN KEY (`supervisor_id`) REFERENCES `users` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_updated_by` FOREIGN KEY (`updated_by_user_id`) REFERENCES `users` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_wallet_account` FOREIGN KEY (`wallet_account_id`) REFERENCES `wallet_accounts` (`id`) ON DELETE SET NULL,
  CONSTRAINT `fk_subscription_orders_wallet_transaction` FOREIGN KEY (`wallet_transaction_id`) REFERENCES `wallet_transactions` (`id`) ON DELETE SET NULL,
  CONSTRAINT `chk_subscription_orders_amount_non_negative` CHECK (((`base_price` >= 0) and (`discount_amount` >= 0) and (`additional_charge` >= 0) and (`final_amount` >= 0))),
  CONSTRAINT `chk_subscription_orders_duration_positive` CHECK (((`tenure_months` > 0) and (`duration_days` > 0))),
  CONSTRAINT `chk_subscription_orders_status` CHECK ((`status` in (_utf8mb4'PAID',_utf8mb4'RECONCILED',_utf8mb4'REJECTED',_utf8mb4'CANCELED'))),
  CONSTRAINT `chk_subscription_orders_type` CHECK ((`order_type` in (_utf8mb4'NEW',_utf8mb4'UPGRADE')))
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
*************************** 1. row ***************************
       Table: subscription_packages
Create Table: CREATE TABLE `subscription_packages` (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT,
  `code` varchar(40) COLLATE utf8mb4_unicode_ci NOT NULL,
  `name` varchar(120) COLLATE utf8mb4_unicode_ci NOT NULL,
  `level_order` int NOT NULL,
  `description` text COLLATE utf8mb4_unicode_ci,
  `active` tinyint(1) NOT NULL DEFAULT '1',
  `created_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `deleted_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uq_subscription_packages_code` (`code`),
  KEY `idx_subscription_packages_active_order` (`active`,`level_order`)
) ENGINE=InnoDB AUTO_INCREMENT=7 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
